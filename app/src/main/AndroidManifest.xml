<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StateMachine">

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activities.AmountActivity" />
        <activity
            android:name=".ui.activities.CardActivity" />
        <activity
            android:name=".ui.activities.AuthActivity" />
        <activity
            android:name=".ui.activities.CompleteActivity" />
        <activity
            android:name=".ui.activities.DeclineActivity" />
        <activity
            android:name=".ui.activities.FailActivity" />

    </application>

</manifest>